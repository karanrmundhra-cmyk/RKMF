# RKM Foundation — P1 Batch 1: Engineering, Security & Infra

**Scope:** Fact-independent P1s — depend only on the codebase, not on RKM content facts (so they proceed while §0 impact numbers, board, and campaign answers are gathered).
**Method:** Every row backed by code inspection (file/line). Generated 24 Jun 2026 against the repo at `rkm-foundation/`.
**Priority frame:** §2 hierarchy. These mostly serve Trust (#3), Performance (#7), Accessibility (#8), SEO (#9) — plus the §12/§13 compliance gates that block the §16 Definition of Done.

> `[ASSUMPTION]` Direction = "full audit, all three." `[ASSUMPTION]` "OK" → start on engineering P1s in parallel while content facts are gathered. Logged in the register below.

---

## The 10 P1s

| # | Recommendation | Why it matters | Expected impact | Files affected | Components | Cx | Priority | How we'll verify |
|---|---|---|---|---|---|---|---|---|
| 1 | **Remove the strict-CSP violation.** `next.config.mjs` ships `script-src 'unsafe-inline' 'unsafe-eval'` and `style-src 'unsafe-inline'`. Move to a **per-request nonce** policy; the inline JSON-LD (`layout.tsx:31`) and any inline style must carry the nonce. Drop `'unsafe-eval'` (GSAP/Framer don't need it at runtime). | §13 hard requirement; this is the single biggest security gap on a money + PII site. `unsafe-inline` defeats XSS protection on the donate page. | Mozilla Observatory/securityheaders → target A+; eliminates the §13 blocker on Definition of Done. | `next.config.mjs:6-15`, `app/layout.tsx:31` | RootLayout, middleware (new) | M | **P1** | securityheaders.com = A+; CSP has no `unsafe-*`; donate + Razorpay checkout still function (manual smoke + E2E from #9). |
| 2 | **Self-host Inter + Noto Sans Devanagari via `next/font/local`.** Currently both load from `fonts.googleapis.com` (`layout.tsx:27-30`) — render-blocking external CSS, FOIT/CLS risk, **leaks every visitor's IP to Google (DPDP/GDPR issue, §13)**, and is *why* `style-src 'unsafe-inline'` is needed. | §14 (self-host, no third-party font CDN) + §13 privacy. Removing Google Fonts also unblocks the strict CSP in #1. | LCP improvement (no extra RTT/render-block); CLS ↓ via `size-adjust`; one fewer third-party in privacy review. | `app/layout.tsx:27-30`, `tailwind.config.ts:12`, `app/globals.css` | RootLayout | M | **P1** | WebPageTest waterfall shows fonts served first-party; lab CLS ≤ 0.05; no request to `fonts.g*` in network panel; lab LCP ≤ 2.0s mobile. |
| 3 | **Fix the false trust signal in schema.org.** `layout.tsx` declares `nonprofitStatus: "Nonprofit501c3"` — a **US tax status** on an Indian charitable trust. Replace with correct `@type: NGO` + `NonprofitType` and Indian registration (12A/80G/CSR-1), and add `DonateAction`. | §9 (every trust claim must be true) + §15 (correct JSON-LD). A wrong legal status is a credibility and compliance risk, not just an SEO miss. | Passes Google Rich Results as NGO; removes a literally-false statutory claim. | `app/layout.tsx:31-40` | RootLayout | S | **P1** | Google Rich Results Test = valid NGO/Organization + DonateAction, no errors; legal text matches receipt PDF (Reg E-30560, 12A 47522, 80G CITE80G…). |
| 4 | **Install privacy-first analytics + instrument the donation funnel.** No analytics in the codebase → **no baselines exist**, so no §2b/§12 conversion target can be measured. Add cookieless, IP-anonymised analytics (Plausible/Fathom or server-side) and emit funnel events: `view → start → amount_selected → method_selected → completed`. | §12 makes this mandatory ("no conversion claim without a funnel metric"). Everything in the donation audit is unfalsifiable until this lands. | Establishes the baseline the entire conversion workstream measures lift against. | `app/layout.tsx`, `app/api/donate/route.ts:1-49`, `components/DonateWidget.tsx`, `components/ImpactSlider.tsx` | DonateWidget, webhook route | M | **P1** | Funnel dashboard shows drop-off per step for ≥7 days; no cookie set pre-consent (verify in DevTools Application tab); events fire in staging E2E. |
| 5 | **Purge 357 committed junk files & add guards.** `git ls-files` returns **357** `.fuse_hidden*`, `.bak`, `.DS_Store`, and `untitled folder` entries (e.g. `app/.fuse_hidden…`, `components/prototype-v2/WhatWeDo.tsx.bak-…`). These ship duplicate/stale source and pollute every audit. | §4 (codebase is source of truth — it must be trustworthy) + §10. Stale `.bak`/`.fuse_hidden` copies risk shipping wrong content and obscure real diffs. | Clean tree; faster, accurate future audits; smaller repo. | repo-wide; `.gitignore:1` | — | S | **P1** | `git ls-files \| grep -iE 'fuse_hidden\|\.bak\|DS_Store' \| wc -l` = 0; `.gitignore` covers them; build still green. |
| 6 | **Add CI quality gates (currently zero).** No `.github/workflows`, no tests, no Lighthouse-CI, no axe. §12 requires build-failing gates. Add: Lighthouse-CI (perf ≥95 mobile, a11y=100), axe-core, and the performance budgets (JS ≤150KB gz, CSS ≤50KB, donate path ≤500KB) enforced on every PR. | §12 + §2b ("budgets enforced in CI — regressions fail the build"). Without gates, every fix below can silently regress. | Locks in the gains from #1–#4; prevents regression on the donate path. | `.github/workflows/*` (new), `package.json:5-9`, `lighthouserc.json` (new) | CI | M | **P1** | A PR that breaches a budget or a11y rule fails CI; green run on main with all gates active. |
| 7 | **Add a critical-path E2E for the full donation flow.** No Playwright/Cypress exists. §12 mandates an E2E covering view→amount→method→Razorpay→thank-you, plus the non-happy states from §8 (declined card, network failure, idempotent retry, 3DS/OTP). | §12 + §8. The donation flow is the revenue path and is currently untested; a silent break = lost donations with no alarm. | Regressions in the money path caught before deploy; documents the required non-happy states. | `e2e/donate.spec.ts` (new), `app/donate-now/`, `app/api/donate/route.ts`, `app/api/razorpay-webhook/` | DonateWidget, webhook | L | **P1** | Playwright run green in CI against Razorpay test mode incl. declined-card + duplicate-event (idempotency) cases. |
| 8 | **Replace 12 raw `<img>` with `next/image`.** Zero `next/image` usage; 12 raw `<img>` across app/components. No responsive `srcset`, no AVIF/WebP, no width/height → CLS, oversized transfers on mobile. | §14 image craft. Images are the LCP element on a photo-led animal-welfare site — this directly drives LCP/CLS, two of the three Core Web Vitals. | LCP ↓ (AVIF/WebP + correct sizing); CLS ↓ (explicit dimensions); smaller mobile transfer. | all `<img>` sites (12), incl. `components/prototype-v2/*`, hero/story components | EditorialFigure, WhatWeDo, ToblerStory, etc. | M | **P1** | Lighthouse "properly sized / next-gen images" pass; LCP image is AVIF with `fetchpriority=high`; lab CLS ≤ 0.05. |
| 9 | **Add automated dependency + secret scanning.** No Dependabot/Renovate, no gitleaks. The handover flags multiple **live secrets pasted in chat** needing rotation — there's no automated guard preventing a future commit of a key, and `next 14.2.21` will drift. | §12 + §13 ("never expose secrets"). Donor-PII + payments raise the bar; secret leakage is the worst-case incident. | Continuous CVE alerts; blocks accidental secret commits. | `.github/dependabot.yml`, gitleaks workflow (new) | CI | S | **P1** | gitleaks runs on every PR and blocks a planted test secret; Dependabot opens PRs for advisories; `npm audit` clean or triaged. |
| 10 | **Confirm `prefers-reduced-motion` coverage across the GSAP/Lenis/Framer layer.** 36 references exist (good signal), but Lenis smooth-scroll + GSAP scroll-triggers are easy to miss. Audit every animated component for a reduced-motion branch and ensure motion never delays LCP (§7). | §7 + §2b a11y (SC respect for reduced motion). Lenis hijacking scroll without a reduced-motion off-switch is a common WCAG/AAA failure and a nausea trigger. | Verifiable a11y compliance on the motion layer; LCP not gated on JS animation. | `lib/*` (Lenis/GSAP init), all Framer components, `app/globals.css` | smooth-scroll provider, hero | M | **P1** | With OS "reduce motion" on: Lenis disabled, no transform-driven entrance animations, LCP unchanged; manual + axe pass. |

---

## Open Questions / Assumptions register

- `[ASSUMPTION]` You want me to proceed on engineering P1s now (interpreting "OK"). If you'd rather I wait for §0 facts and lead with design, say so.
- **Still blocked (content facts), from §0:** (1) one-sentence mission; (2) **real impact numbers + verified cost-per-outcome** — current ₹2,500/₹5,000/₹10,000 tiers read as illustrative, not sourced; (3) NRI/diaspora + multi-currency in scope?; (4) operating city/region (schema says Thane West, Maharashtra — confirm); (5) named founders/board; (6) any existing KPI baselines (assume zero until #4 above lands); (7) live matching-gift campaign? (8) cleared real photography + whether Inter-only is intentional (I recommend adding a display/editorial face per §6a).
- **Note for later batches:** the `prototype` and `prototype-v2` directories appear to be dead/experimental routes shipping in the tree — flag for a "delete or gate" decision in Batch 2 (repo hygiene continuation).
