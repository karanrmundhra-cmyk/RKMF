import type { Metadata } from "next";
import Reveal from "@/components/Reveal";
import CTABanner from "@/components/CTABanner";
import { TESTIMONIALS, SITE } from "@/lib/content";

export const metadata: Metadata = {
  title: "Media",
  description: "The donors, volunteers, vets, and neighbours behind every RKM Foundation rescue — in their own words.",
};

const UPDATES = [
  {
    title: "Photo & video updates",
    desc: "Donors receive periodic photos and short videos from the field, so you can see the rescued animals your support feeds and treats.",
  },
  {
    title: "Field visit invitations",
    desc: "We welcome supporters to feeding drives and shelter visits. When you can't attend in person, we include you over a video call.",
  },
  {
    title: "Annual transparency reports",
    desc: "We file Form 10BD and issue 10BE certificates each year, and share how funds were used — clear governance, responsible reporting.",
  },
];

export default function MediaPage() {
  return (
    <>
      {/* Hero */}
      <section className="bg-snow pb-14 pt-32 sm:pt-40">
        <div className="container-c max-w-3xl">
          <Reveal>
            <p className="eyebrow">Media</p>
            <h1 className="h-display mt-3 text-4xl sm:text-5xl lg:text-6xl">The people who show up for animals.</h1>
            <p className="mt-5 text-lg leading-relaxed text-ink/70">
              The donors, volunteers, veterinarians, and neighbours behind every rescue and every
              meal — telling you, in their own words, why they keep coming back.
            </p>
            <div className="mt-7 flex flex-wrap gap-4 text-sm">
              <a href={SITE.social.instagram} target="_blank" rel="noopener noreferrer" className="link-secondary">Instagram</a>
              <a href={SITE.social.facebook} target="_blank" rel="noopener noreferrer" className="link-secondary">Facebook</a>
              <a href={SITE.social.linkedin} target="_blank" rel="noopener noreferrer" className="link-secondary">LinkedIn</a>
            </div>
          </Reveal>
        </div>
      </section>

      {/* Testimonials grid */}
      <section className="bg-white py-20 sm:py-28">
        <div className="container-c">
          <Reveal>
            <p className="eyebrow">In their own words</p>
            <h2 className="h-display mt-3 text-3xl sm:text-4xl lg:text-5xl">The people behind the work.</h2>
          </Reveal>
          <div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
            {TESTIMONIALS.map((t, i) => (
              <Reveal key={t.name} delay={(i % 3) * 80}>
                <figure className="card flex h-full flex-col p-6">
                  <svg width="26" height="26" viewBox="0 0 24 24" className="text-copper" fill="currentColor" aria-hidden>
                    <path d="M9 6H5.8A2.8 2.8 0 0 0 3 8.8v3.4A2.8 2.8 0 0 0 5.8 15H7v1.5A2.5 2.5 0 0 1 4.5 19H4v2h.5A4.5 4.5 0 0 0 9 16.5V6Zm11 0h-3.2A2.8 2.8 0 0 0 14 8.8v3.4a2.8 2.8 0 0 0 2.8 2.8H18v1.5a2.5 2.5 0 0 1-2.5 2.5H15v2h.5a4.5 4.5 0 0 0 4.5-4.5V6Z" />
                  </svg>
                  <blockquote className="mt-4 flex-1 text-sm leading-relaxed text-ink/80">&ldquo;{t.quote}&rdquo;</blockquote>
                  <figcaption className="mt-5 flex items-center gap-3">
                    <img src={t.img} alt={`Portrait of ${t.name}`} width={44} height={44} className="h-11 w-11 rounded-full object-cover" loading="lazy" />
                    <div>
                      <div className="text-sm font-semibold">{t.name}</div>
                      <div className="text-xs text-ink/60">{t.place}</div>
                      <div className="text-xs font-medium text-copper-dark">{t.context}</div>
                    </div>
                  </figcaption>
                </figure>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      {/* From the Field — future-ready photo gallery placeholder */}
      <section className="bg-snow py-20 sm:py-28">
        <div className="container-c">
          <Reveal>
            <p className="eyebrow text-center">From the Field</p>
            <h2 className="h-display mt-3 text-center text-3xl sm:text-4xl lg:text-5xl">A gallery in the making.</h2>
            <p className="mx-auto mt-4 max-w-xl text-center leading-relaxed text-ink/70">
              Real rescue and shelter photos will live here.
            </p>
          </Reveal>
          <div className="mt-12 grid grid-cols-2 gap-4 sm:grid-cols-3">
            {Array.from({ length: 6 }).map((_, i) => (
              <Reveal key={i} delay={(i % 3) * 80}>
                <div
                  className="flex aspect-square flex-col items-center justify-center rounded-2xl bg-white/70 text-center ring-1 ring-ink/[0.08]"
                  role="img"
                  aria-label="Placeholder — a field photo will appear here"
                >
                  <svg width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6" className="text-ink/30" aria-hidden>
                    <path d="M3 8.5A1.5 1.5 0 0 1 4.5 7h2L8 5h8l1.5 2h2A1.5 1.5 0 0 1 21 8.5v9A1.5 1.5 0 0 1 19.5 19h-15A1.5 1.5 0 0 1 3 17.5v-9Z" strokeLinecap="round" strokeLinejoin="round" />
                    <circle cx="12" cy="12.5" r="3.2" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                  <span className="mt-3 text-[10px] font-semibold uppercase tracking-[0.18em] text-ink/60">Photo coming soon</span>
                </div>
              </Reveal>
            ))}
          </div>
          <p className="mt-6 text-center text-xs text-ink/60">
            These are placeholders — real rescue and shelter photography will be added here.
          </p>
        </div>
      </section>

      {/* How we keep you in the loop */}
      <section className="bg-white py-20 sm:py-28">
        <div className="container-c">
          <Reveal>
            <p className="eyebrow text-center">How We Keep You in the Loop</p>
            <h2 className="h-display mt-3 text-center text-3xl sm:text-4xl lg:text-5xl">What every supporter receives.</h2>
            <p className="mx-auto mt-4 max-w-xl text-center leading-relaxed text-ink/70">
              Support should never feel like a black box. Here is what we share with you, on a regular basis.
            </p>
          </Reveal>
          <div className="mt-12 grid gap-5 sm:grid-cols-3">
            {UPDATES.map((u, i) => (
              <Reveal key={u.title} delay={i * 80}>
                <div className="card h-full p-6">
                  <div className="grid h-10 w-10 place-items-center rounded-full bg-copper/15 text-copper-dark" aria-hidden>
                    <span className="text-sm font-bold">{i + 1}</span>
                  </div>
                  <h3 className="mt-4 text-lg font-semibold">{u.title}</h3>
                  <p className="mt-2 text-sm leading-relaxed text-ink/70">{u.desc}</p>
                </div>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      <CTABanner
        lead="There&apos;s an animal whose story isn&apos;t written yet. You could be the reason it has a happy ending."
      />
    </>
  );
}
