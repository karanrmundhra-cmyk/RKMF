import { redirect } from "next/navigation";

// Retired motion-reference route — its effects now live in production.
// Redirects to the live homepage.
export default function PrototypeV2Retired() {
  redirect("/");
}
